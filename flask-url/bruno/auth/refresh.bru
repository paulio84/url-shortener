meta {
  name: Refresh Token
  type: http
  seq: 3
}

post {
  url: {{baseUrl}}/api/auth/refresh
  body: none
  auth: bearer
}

auth:bearer {
  token: {{refreshToken}}
}

script:post-response {
  if (res.status === 200) {
    bru.setVar("accessToken", res.body.access_token);
  }
}
